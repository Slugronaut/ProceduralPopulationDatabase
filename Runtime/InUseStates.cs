namespace ProceduralPopulationDatabase
{
    /// <summary>
    /// The possible states of each entry within a population.
    /// </summary>
    public enum InUseStates
    {
        Either,
        NotInUse,
        InUse,
    }
}
